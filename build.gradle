/// `/build.gradle´ - top-level build file
///
/// See `/1-BUILD.txt´.
/// 
/// Sync'ed 2017-12-03 with
///   (info "(android) `top.gradle´")

task wrapper (type:Wrapper) {
  gradleVersion = '4.3.1'
  distributionUrl = 
    'https://services.gradle.org/distributions/gradle-4.3.1-all.zip'
}

buildscript {
  repositories {
    google()
    jcenter()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:3.0.1'
  }
}

allprojects {
  repositories {
    google()
    jcenter()
  }
}

